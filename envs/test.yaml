name: test
channels:
    - conda-forge
dependencies:
    - python=3.10 # upgrade to Python 3.11 abd pytest 7.2 blocked due to a problem with "py" in pytest-html
    - numpy=1.24.1
    - pandas=1.5.2
    - pyarrow=10.0.1
    - arviz=0.14.0
    - pytest=7.1.3
    - pytest-html=3.2.0
    - pip=20.1.1
    - pip:
        - pytest-pythonpath # adds the root path "./" to PYTHONPATH
        - datatest==0.11.1
