name: test
channels:
    - conda-forge
dependencies:
    - python=3.10
    - numpy=1.21.5
    - pandas=1.3.5
    - pyarrow=7.0.0
    - pytest=6.2.5
    - pytest-html=2.1.1
    - pip=20.1.1
    - pip:
        - pytest-pythonpath # adds the root path "./" to PYTHONPATH
